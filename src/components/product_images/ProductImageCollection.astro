---
import ProductImageList from './ProductImageList.astro';
import ProductImageMain from './ProductImageMain.astro';

// const normalMainImageId = 'main-image-normal';
// const normalListItemNames = 'product-item-normal';

const { mainImageId, listItemNames, canEnableOverlay } = Astro.props;
---

<style>
  .product-image-collection {
    display: flex;
    flex-direction: column;
    gap: 2rem;

    min-height: 565px;
    width: 100%;
    max-width: 445px;
  }
</style>

<div class='product-image-collection'>
  <ProductImageMain
    mainImageId={mainImageId}
    canEnableOverlay={canEnableOverlay}
  />
  <ProductImageList
    mainImageId={mainImageId}
    listItemClassNames={listItemNames}
  />
</div>
